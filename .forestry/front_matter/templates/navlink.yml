---
label: Navlink
hide_body: true
display_field: label
fields:
- name: label
  type: text
  config:
    required: false
  label: Лейбл
- name: slug
  type: text
  config:
    required: false
  label: Ссылка
- name: sub_links
  type: blocks
  template_types:
  - navlink
  config:
    min: 
    max: 
  label: Суб-ссылки
- name: page
  type: blocks
  label: Страница
  template_types:
  - page
  config:
    min: 
    max: 1
